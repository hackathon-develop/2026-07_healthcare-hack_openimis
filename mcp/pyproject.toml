[project]
name = "openimis-mcp"
version = "0.1.0"
description = "Read-only MCP server exposing parameterized query tools over an openIMIS PostgreSQL database"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "psycopg2-binary>=2.9",
]
